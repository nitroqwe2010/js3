let person = {
    name: null,
    id : null,
    password : null,
}

person.name = prompt('name  kiriting');
person.id = prompt('id kiriting');
person.password = prompt('password kiriting');

console.log(person);
